#include <iostream>
using namespace std;

int main()
{
    cout << "auto, goto, typedef, explicit, static_cast," << endl;
    cout << "double, do, union, false, template," << endl;
    cout << "break, if, unsigned, friend, this," << endl;
    cout << "else, int, void, inline, throw," << endl;
    cout << "enum, register, while. namespace, try," << endl;
    cout << "char, return, asm, new, typeid," << endl;
    cout << "extern, short, bool, operator, typename," << endl;
    cout << "const, signed, catch, private, using," << endl;
    cout << "float, sizeof, class, protected, virtual," << endl;
    cout << "continue, static, const_cast, public, wchar_t," << endl;
    cout << "for, struct, delete, reinterpet_cast," << endl;
    cout << "default, switch, dynamic_cast" << endl;

    return 0;
}
